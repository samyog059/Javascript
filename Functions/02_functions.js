// A function named "AddNum" that takes two numbers as input
function AddNum(num1, num2) {
    return num1+num2
}

// Calling the function and passing 3 and 9 as arguments
const result = AddNum(3, 9);   // Output: 12
console.log("The sum of two number is:"+result);

